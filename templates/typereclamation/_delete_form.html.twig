<form method="post" action="{{ path('typereclamation_delete', {'idtypereclamation': typereclamation.idtypereclamation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typereclamation.idtypereclamation) }}">
    <button class="btn">Delete</button>
</form>
