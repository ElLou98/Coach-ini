{% extends 'frontCoach.html.twig' %}

{% block slider %}
{% endblock %}

{% block title %}Planning Client{% endblock %}

{% block change%}
    <h1>SeancePlanning</h1>

    <table class="table table-responsive">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ seance_planning.id }}</td>
            </tr> #}
            <tr>
                <td><strong>username:</strong> <i> {{ seance_planning.userName }}</i></td>
            </tr>
            <tr>
                <td><strong>Summary:</strong> <i> {{ seance_planning.summary }}</i></td>
            </tr>
            <tr>
                <td><strong>Description:</strong> <i> {{ seance_planning.description }}</i></td>
            </tr>
            <tr>
                <td><strong>Date:</strong> <i> {{ seance_planning.date }}</i></td>
            </tr>
            <tr>
                <td><strong>StartsAt:</strong> <i> {{ seance_planning.startsAt }}</i></td>
            </tr>
            <tr>
                <td><strong>FinishsAt:</strong> <i> {{ seance_planning.finishsAt }}</i></td>
            </tr>
            <tr>
                <td><strong>Localisation:</strong> <i> {{ seance_planning.localisation }}</i></td>
            </tr>


        </tbody>
    </table>




    <div style='display:inline-block;margin-left:10px; '>
        <a href="{{ path('seance_planning_index') }}"class="btn btn-primary">back to list</a>

        <a href="{{ path('seance_planning_edit', {'id': seance_planning.id}) }}"class="btn btn-info">edit</a>


    </div>
    <div style='display:inline-block; margin-right:10px;'>
        {{ include('seance_planning/_delete_form.html.twig') }}
    </div>

{% endblock %}
