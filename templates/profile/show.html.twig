{% extends 'baseFront.html.twig' %}

{% block title %}Profil{% endblock %}

{% block slider %}
{% endblock %}
    {% block change %}
        <div class="container">

    <h1> Profile  <button type="button" class="btn btn-google " name="button" style="margin-left: 100px; color:#8b0000;"> <a href="{{ path('listprofil', {'id': profile.id}) }}"> PDF </a> </button></h1>


    <table class="table">
        <tbody>

            <tr>
                <th>Id</th>
                <td>{{ profile.id }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src="/ProfileImages/{{ asset(profile.photo)  }}" width="200" height="200"></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ profile.description }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ profile.rating }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ profile.categorie }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ profile.detail }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ profile.nom }}</td>
            </tr>
            <tr>
                <th>Id_compte</th>
                <td>{{ profile.idCompte }}</td>
            </tr>
        </tbody>
    </table>



            <div style="display: flex;width: 250px;justify-content: space-between;align-items: center">
                <a style="width: 100px" class="btn btn-warning mt-1" href="{{ path('profile_edit', {'id': profile.id}) }}">edit</a>

                    {{ include('profile/_delete_form.html.twig') }}


            </div>

            <a class="mt-1" href="{{ path('profile_index') }}">back to list</a>
        </div>
{% endblock %}
