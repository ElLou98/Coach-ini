{% extends 'baseFront.html.twig' %}
{% block title %}Profile{% endblock %}

{% block slider %}
{% endblock %}
    {% block change %}
        <div class="container" style="display: flex;align-items: center;justify-content: center;flex-direction: column;font-size: 18px">

            {% for profile in profiles %}
                <h1>{{ profile.nom }}</h1>
            <img src="/ProfileImages/{{ asset(profile.photo)  }}" style="width: 170px;height: 170px;border-radius: 50px;margin: 20px">

                {#<div style="width: 400px;display: flex;align-items: center;justify-content: space-between;">
                    <div >Name :</div>
                    <div style="display: flex;width: 45%;align-items: center;justify-content: left">{{ profile.nom }}</div>
                </div>#}
                <div style="width: 400px;display: flex;align-items: center;justify-content: space-between;">
                    <div>Description : </div>
                    <div style="display: flex;width: 45%;align-items: center;justify-content: left">{{ profile.description }}</div>
                </div>
                <div style="width: 400px;display: flex;align-items: center;justify-content: space-between;">
                    <div>Rating : </div>
                    <div style="display: flex;width: 45%;align-items: center;justify-content: left"> {{ profile.rating }}</div>
                </div>
                <div style="width: 400px;display: flex;align-items: center;justify-content: space-between;">
                    <div>Category : </div>
                    <div style="display: flex;width: 45%;align-items: center;justify-content: left">{{ profile.categorie }}</div>
                </div>

                <div style="width: 400px;display: flex;align-items: center;justify-content: space-between;">
                    <div>Detail : </div>
                    <div style="display: flex;width: 45%;align-items: center;justify-content: left">{{ profile.detail }}</div>
                </div>

                <div style="display: flex;width: 250px;align-items: center;justify-content: space-between;margin-top: 20px;margin-bottom: 20px">
                    <a style="width: 100px" class="btn btn-info" href="{{ path('profile_show', {'id': profile.id}) }}">show</a>
                    <a style="width: 100px" class="btn btn-warning" href="{{ path('profile_edit', {'id': profile.id}) }}">edit</a>
                </div>
            {% endfor %}
   {# <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Photo</th>
                <th>Description</th>
                <th>Rating</th>
                <th>Categorie</th>
                <th>Detail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profile in profiles %}
            <tr>
                <td>{{ profile.nom }}</td>
                <td><img src="/ProfileImages/{{ asset(profile.photo)  }}" width="200" height="200"> </td>
                <td>{{ profile.description }}</td>
                <td>{{ profile.rating }}</td>
                <td>{{ profile.categorie }}</td>
                <td>{{ profile.detail }}</td>
                <td>
                    <a class="btn btn-info" href="{{ path('profile_show', {'id': profile.id}) }}">show</a>
                    <a class="btn btn-warning" href="{{ path('profile_edit', {'id': profile.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>#}

        </div>
        {% endblock %}