<form method="post" action="{{ path('utilisateurbloque_delete', {'idbloque': uBloque.idbloque}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uBloque.idbloque) }}">
    <button class="btn btn-success">Débloquer</button>
</form>
