{% extends 'base.html.twig' %}

{% block title %}Utilisateurbloque{% endblock %}

{% block body %}
    <h1>Utilisateurbloque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idbloque</th>
                <td>{{ utilisateurbloque.idbloque }}</td>
            </tr>
            <tr>
                <th>Utilisateur</th>
                <td>{{ utilisateurbloque.utilisateur }}</td>
            </tr>
            <tr>
                <th>Abloque</th>
                <td>{{ utilisateurbloque.abloque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('utilisateurbloque_index') }}">back to list</a>

    <a href="{{ path('utilisateurbloque_edit', {'idbloque': utilisateurbloque.idbloque}) }}">edit</a>

    {{ include('utilisateurbloque/_delete_form.html.twig') }}
{% endblock %}
