{% extends 'baseFront.html.twig' %}

{% block slider %}
{% endblock %}

{% block change %}

    {{ include('message/_form.html.twig', {'button_label': 'Valider'}) }}
    <div class="mt-5 mb-5">
        <div class="row mt-5 ">
            <div class="col offset-sm-1 mt-5">
                {{ include('message/_delete_form.html.twig') }}
            </div>

        </div>
    </div>


{% endblock %}
