
{% block change%}
    <form method="post" action="{{ path('review_delete_client', {'idReview': review_client.idReview}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review_client.idReview) }}">
        <button class="btn btn-danger">Delete</button>
    </form>
{% endblock %}
